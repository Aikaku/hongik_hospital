spring:
  datasource:
    # H2 ?????? ?? URL (tcp ??, ??? ? ????? jpashop DB ??
    # MVCC ???? ??? ??? H2 2.xx ??? ???? ?? ??
    url: jdbc:h2:tcp://localhost/~/hongik_hospital
    # url: jdbc:h2:mem:test -> ?? ?? ??? ? ?? ?? ??? ??
    # DB ?? ?? ID
    username: sa
    # DB ?? ???? (H2 ??? ????)
    password:
    # H2 ?????? ???? ???
    driver-class-name: org.h2.Driver

  jpa:
    hibernate:
      # ?????? ?? ? ??? ?? ?? (create: ?? ?? ??? ?? ? ?? ??)
      ddl-auto: create
    properties:
      hibernate:
        # ???? SQL? ??? ??
        # show_sql: true
        # SQL?? ?? ?? ????? ??
        format_sql: true

logging:
  level:
    # Hibernate? ???? SQL ??? DEBUG ??? ??
    org.hibernate.SQL: debug
    org.hibernate.orm.jdbc.bind: trace

